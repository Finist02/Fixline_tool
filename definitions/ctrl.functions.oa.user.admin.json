{
  "hover": {
    "format": "```\n${definition}\n```\n\n-------\n\nPredefined WinCC OA function\n\n${description}"
  },
  "completionItemKind": "Function",
  "defines": [
    {
      "definition": "int setScriptUserId(unsigned uid)",
      "description": [],
      "id": "setScriptUserId"
    },
    {
      "definition": "int setScriptLangId(unsigned id)",
      "description": [],
      "id": "setScriptLangId"
    },
    {
      "definition": "bool setUserId(unsigned uid [,string password])",
      "description": [],
      "id": "setUserId"
    },
    {
      "definition": "unsigned getUserId([string name])",
      "description": [
        "Returns the associated ID for the specified user name."
      ],
      "id": "getUserId"
    },
    {
      "definition": "string getUserName([unsigned id])",
      "description": [
        "Returns the associated user name for a specified ID."
      ],
      "id": "getUserName"
    },
    {
      "definition": "boolean getUserPermission(int level [, unsigned userid, int manType, int manNum])",
      "description": [
        "Checks authorizations."
      ],
      "id": "getUserPermission"
    },
    {
      "definition": "bool checkPassword(unsigned id, string passwd)",
      "description": [],
      "id": "checkPassword"
    }
  ]
}