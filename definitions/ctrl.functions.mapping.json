{
    "hover": {
        "format" : "```\n${definition}\n```\n\n-------\n\nPredefined WinCC OA mapping functions\n\n${description}"
    },
    "completionItemKind": "mapping",
    "defines":[
        
        {
            "definition": "int mapping.clear();",
            "description": [
                "Empties the mapping."
            ],
            "id": "clear"
        },
        {
            "definition": "bool mapping.contains(T key);",
            "description": [
                "Returns true if the key is already in the mapping."
            ],
            "id": "contains"
        },
        {
            "definition": "int mapping.count();",
            "description": [
                "Returns the number of keys."
            ],
            "id": "count"
        },
        {
            "definition": "int mapping.insert(T key, T value);",
            "description": [
                "Insert a key/value pair."
            ],
            "id": "insert"
        },
        {
            "definition": "int mapping.isEmpty();",
            "description": [
                "Returns true if the mapping is empty."
            ],
            "id": "isEmpty"
        },
        {
            "definition": "int mapping.merge(mapping other);",
            "description": [
                "Merges two mappings."
            ],
            "id": "merge"
        },
        {
            "definition": "T mapping.keyAt(uint idx);",
            "description": [
                "Returns the key at the given mapping index."
            ],
            "id": "keyAt"
        },
        {
            "definition": "mapping mapping.merged(mapping other);",
            "description": [
                "Creates a new instance from merging two mappings."
            ],
            "id": "merged"
        },
        {
            "definition": "T mapping.value(T key, T defaultValue = anytype());",
            "description": [
                "Returns the value at given key."
            ],
            "id": "value"
        },
        {
            "definition": "T mapping.valueAt(uint idx);",
            "description": [
                "Returns the value at the given mapping index."
            ],
            "id": "valueAt"
        }
    ]
}